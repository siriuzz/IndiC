import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    paperSmall: {
        marginTop: "30px",
        marginLeft: "40px",
        borderRadius: "25px",
        width: "608px",
        height: "650px",
        alignItems: "center",
        display: "flex",
        flexDirection: "column"
    },
})
